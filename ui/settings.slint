export component settingsView
{ 
    VerticalLayout {
        alignment: center;
        
        Text {
            text: "Settings";
            horizontal-alignment: center;
            letter-spacing: 5px;
            font-size: 80px;
        }


    }
}