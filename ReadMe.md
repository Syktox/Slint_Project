# Setup Project

Be sure that every core component is installed.
- CMake
- C/C++ Extension



When its not running on Mac use this. IDK what it does.

cmake --build build --config Debug --target all -- -j1
